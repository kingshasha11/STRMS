package src.enumeration;

public enum NotificationType {
    EMAIL, // Notifications sent via electronic mail.
    SMS,   // Notifications sent via short message service.
    CONSOLE // Notifications sent via console.
}